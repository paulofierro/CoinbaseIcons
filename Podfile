platform :ios, '12.0'
# Uncomment the lines below if you have the cocoapods-binary gem installed
# and want to get on the speed train.
#plugin 'cocoapods-binary'

target 'Coinbase Icons' do
  use_frameworks!
  #all_binary!
  #enable_bitcode_for_prebuilt_frameworks!
  #keep_source_code_for_prebuilt_frameworks!

  pod 'Kingfisher', '~> 5.0' # https://github.com/onevcat/Kingfisher
  pod 'Kanna', '~> 5.0.0' # https://github.com/tid-kijyun/Kanna

end
